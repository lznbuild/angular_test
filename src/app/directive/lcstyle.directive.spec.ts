import { LcstyleDirective } from './lcstyle.directive';

describe('LcstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new LcstyleDirective();
    expect(directive).toBeTruthy();
  });
});
